---
apiVersion: 1
datasources:
  - name: Prometheus
    orgId: 1
    access: proxy
    editable: true
    isDefault: true
    type: prometheus
    url: http://prometheus.service:{{ config_prometheus.port }}
    version: 1
